# Local development environment variables
# Copy to `.env` (which should stay uncommitted) and fill in your own values.

# Required for running generate_stories.py/backfill_codex.py locally
ANTHROPIC_API_KEY=
